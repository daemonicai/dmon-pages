source "https://rubygems.org"

# Matches the GitHub Pages build environment so `bundle exec jekyll serve`
# locally mirrors what GitHub renders.
gem "github-pages", group: :jekyll_plugins

# Local preview convenience
gem "webrick", "~> 1.8"
